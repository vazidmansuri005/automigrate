/**
 * Robot Framework → Playwright Transformation Rules
 *
 * Maps SeleniumLibrary and AppiumLibrary keywords to Playwright equivalents.
 * Robot Framework uses keyword-based syntax with 2+ space separators.
 */

import type { TransformationRule } from '../types/index.js';

// These rules are for reference/documentation — actual transforms use direct rules
export function generateRobotRules(): TransformationRule[] {
  return [];
}
